# Copy to terraform.tfvars and adjust. All have sensible defaults; override as needed.

aws_region  = "us-east-1"
environment = "demo"

# Keyless GitHub Actions deployment. IDs are public, immutable GitHub metadata;
# including them prevents a renamed/deleted repository from inheriting trust.
github_org           = "kalanas210"
github_repos         = ["durdans-lims"]
github_owner_id      = "123456789"
github_repo_ids      = { durdans-lims = "987654321" }
github_deploy_branch = "main"

# Compute / DB sizing (4 GB avoids JVM contention across app/Keycloak/Kafka)
ec2_instance_type  = "t3.medium"
rds_instance_class = "db.t4g.micro"

# Leave empty to use SSM Session Manager (recommended). Or lock SSH to your IP:
# ssh_ingress_cidr = "203.0.113.4/32"

# Image tags CI pushes to ECR (default "latest")
app_image_tag      = "latest"
frontend_image_tag = "latest"

# Optional custom domain for TLS via Caddy. Empty = serve over the EIP on HTTP.
domain_name = ""

# Cost guardrail — set your email to receive budget alerts at 80% / 100%.
budget_monthly_usd = 60
alert_email        = ""
